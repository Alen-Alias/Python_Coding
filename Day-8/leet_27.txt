class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        r=[]
        for i in nums:
            if i != val:
                r.append(i)
        j=0
        for i in r:
            nums[j]=i
            j+=1
        return len(r)