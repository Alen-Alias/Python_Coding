class Solution:
    def simplifyPath(self, path: str) -> str:
        st=[]
        p=path.split("/")
        for i in p:
            if i=="." or i=="":
                continue
            if i=="..":
                if st:
                     st.pop()
            else:
                st.append(i)
        return "/"+"/".join(st)
            